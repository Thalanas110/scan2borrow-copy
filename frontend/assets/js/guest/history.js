class GuestHistoryController {
    constructor() { this.form = document.getElementById("guest-history-form"); this.body = document.getElementById("guest-history-body"); this.load(); this.form?.addEventListener("submit", (event) => { event.preventDefault(); this.load(); }); }
    escapeHtml(value) { return String(value == null ? "" : value).replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[character])); }
    load() { const query = new URLSearchParams(new FormData(this.form)).toString(); fetch("/scan2borrow/api/guest/history?" + query, { headers: { "X-Requested-With": "fetch" } }).then((response) => response.json()).then((response) => { if (response.ok) this.render(response.data?.history || []); }); }
    render(rows) { this.body.replaceChildren(); const labels = { "Return Verification Pending": "Return Verification", Pending: "Pending Approval", "Ready for Release": "Ready for Release", Released: "Released", Returned: "Returned", Rejected: "Rejected" }; rows.forEach((row) => { const tr = document.createElement("tr"); const receivable = ["Released", "Ready for Release", "Returned"].includes(row.request_status); tr.innerHTML = `<td><strong>${this.escapeHtml(row.title)}</strong><div class="small text-muted">${this.escapeHtml(row.author)}</div>${row.review_notes ? `<div class="small text-muted"><em>${this.escapeHtml(row.review_notes)}</em></div>` : ""}</td><td>${this.escapeHtml(row.borrow_date)}</td><td>${this.escapeHtml(row.due_date)}</td><td>${this.escapeHtml(row.return_date || "—")}</td><td><span class="badge bg-${row.request_status === "Rejected" ? "danger" : row.request_status === "Returned" ? "success" : "warning text-dark"}">${this.escapeHtml(row.status_label || labels[row.request_status] || row.request_status || "Pending")}</span></td><td>${receivable ? `<a class="btn btn-outline-success btn-sm" target="_blank" href="/scan2borrow/guest/receipt?id=${encodeURIComponent(row.id)}">View</a>` : '<span class="text-muted small">—</span>'}</td>`; this.body.appendChild(tr); }); if (!rows.length) this.body.innerHTML = '<tr><td colspan="6" class="text-center text-muted py-4">No matching history.</td></tr>'; }
}
window.addEventListener("DOMContentLoaded", () => new GuestHistoryController());
