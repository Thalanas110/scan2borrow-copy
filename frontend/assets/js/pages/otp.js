class OtpPageController { constructor() { this.csrf = document.querySelector('meta[name="csrf"]')?.content || ""; document.getElementById("otp-form")?.addEventListener("submit", (event) => this.submit(event)); document.getElementById("resend-form")?.addEventListener("submit", (event) => this.resend(event)); } request(form, endpoint) { const body = new FormData(form); body.append("csrf", this.csrf); return fetch(endpoint, { method: "POST", body }).then((response) => response.json()); } submit(event) { event.preventDefault(); this.request(event.currentTarget, "/scan2borrow/api/auth/otp").then((response) => { if (!response.ok) throw new Error(response.errors?.[0] || "Invalid or expired OTP code."); location.href = response.data.redirect || "/scan2borrow/login"; }).catch((error) => this.message("form-error", error.message)); } resend(event) { event.preventDefault(); this.request(event.currentTarget, "/scan2borrow/api/auth/otp/resend").then((response) => this.message(response.ok ? "form-success" : "form-error", response.message || response.errors?.[0] || "Unable to resend OTP.")); } message(id, text) { const node = document.getElementById(id); node.hidden = false; node.textContent = text; } }
window.addEventListener("DOMContentLoaded", () => new OtpPageController());
